{% extends 'base.html.twig' %}

{% block body %}
    <h4>Bonjour </h4>
    {% for user in users %}
        <h5><a href="{{ path('app_user_provider', {userId: user.id}) }}">voir</a></h5>
        <h5>{{ user.firstName }}</h5>
        <h5>{{ user.lastName }}</h5>
        <h5>{{ user.email }}</h5>
        <hr>
    {% endfor %}
{% endblock %}
